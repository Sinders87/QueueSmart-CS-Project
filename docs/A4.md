# Assignment 4: Data Design & Database Implementation (A4)

## 1. GitHub Repository Link

[...]

The repository includes:  
    •    Backend code with database integration  
    •    Updated API routes connected to the database  
    •    Unit tests and coverage reports  
    •    Commit history showing team contributions  


## 2. Database Schema (RDBMS)

We used a Relational Database Management System (RDBMS), specifically SQLite.

    db.run(`
        CREATE TABLE IF NOT EXISTS notifications (
        id INTEGER PRIMARY KEY AUTOINCREMENT,
        userName TEXT NOT NULL,
        role TEXT NOT NULL DEFAULT 'user',
        message TEXT NOT NULL,
        status TEXT NOT NULL DEFAULT 'sent',
        created_at TEXT NOT NULL)
        `);

    db.run(`
        CREATE TABLE IF NOT EXISTS queue_entries (
        id INTEGER PRIMARY KEY,
        serviceId INTEGER NOT NULL,
        userName TEXT NOT NULL,
        status TEXT NOT NULL,
        position INTEGER NOT NULL,
        estimateWait INTEGER NOT NULL,
        joinTime TEXT
        )
    `);

    db.run(`
        CREATE TABLE IF NOT EXISTS services (
        id INTEGER PRIMARY KEY,
        name TEXT NOT NULL,
        description TEXT,
        expectedDuration INTEGER NOT NULL,
        priority TEXT NOT NULL,
        isActive INTEGER NOT NULL
        )
    `);

## 3. Code Coverage Report

Backend unit tests were updated to include database-integrated logic.

Coverage achieved: 
File            | % Stmts | % Branch | % Funcs | % Lines | Uncovered Line #s                
----------------|---------|----------|---------|---------|----------------------------------
All files       |   35.73 |     17.6 |   14.08 |   37.02 |                                  


## 4. Team Contributions

Group Member | Contribution | Discussion Notes
|------------------|---------------------------|-----------------|
Storm | Implemented UserCredentials and UserProfile tables, password hashing with bcrypt, connected authentication routes to database, enforced unique email constraint, updated authentication tests | Focused on authentication and user data
Dennis | Implemented Service, Queue, QueueEntry, and Notification/History tables, connected APIs to database, handled data persistence, added backend validations, wrote unit tests   | Focused on core backend and database logic
Joel | Connected wait-time logic and notification module to database, ensured persistence and status updates, tested admin features   | Focused on advanced features and admin side
Aalakha | Updated frontend to use database APIs, displayed services, queue status, and history from database, tested full user flow, verified integration  | Focused on frontend integration and testing
